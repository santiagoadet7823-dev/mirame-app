<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Supabase, sync y descarga del APK de actualizacion. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- El motor de sync solo drena la cola cuando hay red; sin esto
         connectivity_plus reporta siempre "sin conexion" en Android 12+. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- Auto-updater: instalar el APK descargado desde GitHub Releases. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>
    <!-- Fotos de las prendas y escaneo del codigo de barras. La galeria NO
         necesita permiso: image_picker usa el selector del sistema. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- La app funciona sin camara (se puede elegir de la galeria y tipear el
         codigo), asi que no se exige el hardware: exigirlo la sacaria de Play
         para tablets sin camara trasera. -->
    <uses-feature android:name="android.hardware.camera" android:required="false"/>

    <!-- Recordatorios de retoque y de turno del dia siguiente. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Instalacion sin confirmacion en Android 12+. Es de nivel normal: no
         se pide en runtime, alcanza con declararla. Solo surte efecto si la
         app es su propio installer of record (ver MirameUpdater.kt). -->
    <uses-permission android:name="android.permission.UPDATE_PACKAGES_WITHOUT_USER_ACTION"/>

    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Vuelta del OAuth de Supabase: com.mirame.app://auth
                 Lo captura `app_links` (dependencia de supabase_flutter) sobre
                 esta misma activity, que es `singleTop`, asi que la sesion
                 llega por onNewIntent sin recrear la app.

                 Este scheme tiene que estar TAMBIEN en Supabase >
                 Authentication > URL Configuration > Redirect URLs. Si no
                 coinciden, Google autentica pero la app nunca recibe nada. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="com.mirame.app" android:host="auth"/>
            </intent-filter>
        </activity>
        <!-- Entrega el APK descargado al instalador del sistema en el camino
             de reserva (Android 11 y anteriores). La autoridad tiene que
             coincidir exactamente con la que arma MirameUpdater.kt. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>

        <!-- DECLARADO acá y no registrado en runtime: la instalacion mata el
             proceso de la app, y un receiver de runtime moriria con el justo
             antes de recibir el resultado. -->
        <receiver
            android:name=".InstalacionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.mirame.app.INSTALACION_RESULTADO"/>
            </intent-filter>
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <!-- WhatsApp hacia las clientas y apertura del navegador para el OAuth.
             Sin estos <intent>, canLaunchUrl() devuelve false en Android 11+
             aunque la app este instalada. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="tel"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
